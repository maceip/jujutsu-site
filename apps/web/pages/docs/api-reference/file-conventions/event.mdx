# `event.js`

An **event** is a occurrence dispatched from the Discord API, most times in relation to your Discord bot's surroundings.

```typescript app/hello/command.ts
export default function Ready({ client }: Jujutsu.EventProps) {
  interaction.reply(`Hello, ${client.user?.tag}!`)
}
```

### Props

#### `client`

The Discord bot that you are currently logged into. Learn more [here](https://discord.js.org/docs/discord.js/main/class/Client)
